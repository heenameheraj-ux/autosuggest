# autosuggest


Checking my git set-up : )

Change flow for pushing
1.add(plus button)
2.commit with message
3.push/sync

Command to commit if ui not work 
These commands always works if the setup is correct
1.git add -A
2.git commit -m "some message"
git push